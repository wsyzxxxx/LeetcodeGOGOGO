#include <bits/stdc++.h>
typedef long long LL;

const LL MOD = 998244353LL;
LL T, N, M;
std::vector<std::vector<int>> m;

void solve() {
}

int main() {
    std::cin >> T;

    while (T--) {
        std::cin >> N >> M;
        m = std::vector<std::vector<int>>(N, std::vector<int>(M));
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                std::cin >> m[i][j];
            }
        }
        solve();
    }

    return EXIT_SUCCESS;
}